This project builds a small data pipeline using the TMDB API to collect movie data.
The data is fetched using Python and stored in a pandas DataFrame.
The dataset is then saved into a SQLite database table named movies.
Finally, exploratory data analysis (EDA) is performed to understand the movie data.
